{
  "name": "asset-management-message-ingestion-lambda",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "vitest",
    "test:ci": "vitest --run",
    "build": "esbuild src/index.ts --bundle --minify --outfile=dist/index.js --external:@aws-sdk --external:aws-sdk --platform=node"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "3.899.0",
    "@aws-sdk/credential-providers": "^3.899.0",
    "@guardian/libs": "^26.0.0",
    "esbuild": "^0.25.10"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "vitest": "^3.2.4"
  }
}
